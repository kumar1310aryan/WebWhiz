module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // All JavaScript, TypeScript, and JSX/TSX files in the `src` folder
    "./public/index.html", // Include the `index.html` file if needed
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
